func exit(){
    mov(eax 1)
    int(0x80)
}

file prompt{enter_name_ }
file name  {......................}
file hello {hello_ }

func print(len ptr){
    mov(ecx ptr)
    mov(edx len)
    mov(ebx 1)
    mov(eax 4)
    int(0x80)
}

func read(len ptr):nchars{
    mov(esi ptr)
    mov(edx len)
    xor(eax eax)
    xor(edi edi)
    syscall()
    mov(nchars eax)
}

func test(){
	print(prompt.len prompt)
	var ln=read(name.len name)
	print(hello.len hello)
	print(ln name)
}

func test2(){
	print(hello.len hello)
	test()
}

func op_plus(a b):sum{
	mov(sum a)
	add(sum b)
}

func main(){
	test2()
	var c=op_plus(1 2)
	mov(c op_plus(1 3))
	exit()
}
