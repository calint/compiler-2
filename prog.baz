func exit(v){
    mov(ebx,v)
    mov(eax,1)
    int(0x80)
}

func main{
    var a=1
    var b=2
    var c=3
    var d=4
    var r=((b+c)*d+c*(a+b))*2
#    if (a=1 or b=2) and (c=3 or d=4)
    if (a=1 and b=2) or (c=3 and d=4)
        exit(1)
    exit(r)
}

# comment
