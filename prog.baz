func exit(v){
    mov(ebx,v)
    mov(eax,1)
    int(0x80)
}

func main{
    var a=1;
    var b=2
    var c=3
    var d=4
    var r=
      ((b+c)*d+
      c*(a+b))*2
    if r=58
        exit(0)
    else if r=57
        exit(1)
    else
        exit(r)
}

# comment
