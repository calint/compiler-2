func inline exit(v:reg_rbx){
    mov(rbx,v)
    mov(rax,1)
    int(0x80)
}

func fib(i):res{
    if i=0 {
        res=1
        return
    }
    res=i*fib(i-1)
}

func inline main{
    var a=1
    var b=3
    if a=1 {
      var b=2
    }else if a=2 {
      var b=3
    }else{
      var b=4
    }
    exit(fib(5))
}
