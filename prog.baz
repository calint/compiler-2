func inline exit(v:reg_rbx){
    mov(rbx,v)
    mov(rax,1)
    int(0x80)
}

func fib(i):res{
    if i=0 {
        res=1
        return
    }
    var r=i*fib(i-1)
    res=r
}

func inline main{
    exit(fib(5))
}
