func inline exit(v:reg_rbx){
    mov(rbx,v)
    mov(rax,1)
    int(0x80)
}

func inline f(x:reg_rdx):res{
    res=-x*2
}

func inline main{
    var a=2
    var b=f(-a+1);
    if b=2
        exit(0)
    exit(1)
}
