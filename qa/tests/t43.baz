func inline exit(v:reg_rdi){
    mov(rax,60) # exit system call
    mov(rdi,v)  # return code
    syscall
}

func inline main{
    var a=0xfffffffffffffffe # -1
    var b=2*~a
    if b=2
        exit(0)
    exit(1)
}
