func assert(exit, expr : bool) {
    if not expr exit(exit)
}

func inv(i) : res i32 {
    res = ~i
}

func main() {
    var arr : i32[4]

    arr[1] = ~2
    arr[2] = ~arr[1]
    arr[3] = 1

    var ix = 3

    arr[ix-2] = inv(arr[ix])
    assert(1, arr[1] == ~1)
}
