# signed thirty-two-bit index remains negative

func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}

func main() {
    var arr : i8[8]
    var index : i32 = -1
    arr[index] = 1
}
