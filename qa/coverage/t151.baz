func exit(v : reg_rdi) {
    mov(rax, 60)  # exit system call
    mov(rdi, v)   # return code
    syscall()
}

func assert(expr : bool) if not expr exit(1)

const yes = 1
const no = 0
const maybe = -1

type point { x, y }

func main() {
    dat p1 : point = { true, no }
    assert (p1.x == yes)
    assert (p1.y == no)

    var answer = maybe
    assert(answer == -1)

    answer = yes
    assert(answer == 1)

    var p2 : point = { no, yes }
    assert (p2.x == no)
    assert (p2.y == yes)

}
