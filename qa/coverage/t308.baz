func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}
func assert(x : bool) if not x exit(1)
func main() {
    var destination = 0
    mov(destination, ~-7)
    assert(destination == 6)
}
