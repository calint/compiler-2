func exit(v : reg_rdi) {
    mov(rax, 60)  # exit system call
    mov(rdi, v)   # return code
    syscall()
}

func assert(expr : bool) if not expr exit(1)

type point { x, y }

type msgpoint {  msg : i8[128], pt : point }

func main() {
    var x = -1
    var y = -2

    var pt : point = { x, y }
    assert(pt.x == -1)
    assert(pt.y == -2)

return 
#    var pts1 : point[4]
#    pts1 = { { x, x + y }, { -6, 4 } }
#    assert(pts1[0].x == -1)
#    assert(pts1[0].y == -3)
#    assert(pts1[1].x == -6)
#    assert(pts1[1].y == 4)

 #   var pts2 : point[] = { { x * 2, y }, { 3, 4 } }
 #   assert(pts2[0].x == -2)
 #   assert(pts2[0].y == -2)
 #   assert(pts2[1].x == 3)
 #   assert(pts2[1].y == 4)

 #   var pts3 : point[4] = { { y * 2, x }, { 7, 6 } }
 #   assert(pts3[0].x == -4)
 #   assert(pts3[0].y == -1)
 #   assert(pts3[1].x == 7)
 #   assert(pts3[1].y == 6)

 #   var pts4 : point[4] = { { y * 2 } }
 #   assert(pts4[0].y == 0)

 #   var mp : msgpoint[] = { { {}, { x, y } } }
 #   assert(mp[0].pt.x == -1)
 #   assert(mp[0].pt.y == -2)
}
