# nested negated function calls

func exit(v:reg_rdi){
    mov(rax,60) # exit system call
    mov(rdi,v)  # return code
    syscall()
}

func foozi(x):i64 res{
    res=-x*2
}

func bar(y):i64 res{
    res=-y
}

func main(){
    var a=1
    var b=-foozi(-bar(-a))
    if(b==-2)
        exit(0)
    exit(1)
}
