# constants shadow across nested blocks

func assert(x : bool) if not x exit(1)

const value = 1

func main() {
    assert(value == 1)
    {
        const value = 2
        assert(value == 2)
    }
    assert(value == 1)
}
