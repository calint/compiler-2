func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}

type group { values : i8[2] }

func main() {
    var groups : group[2]
    var outer = 0
    var inner = 2
    groups[outer].values[inner] = 1
}
