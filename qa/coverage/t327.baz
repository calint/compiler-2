# incompatible user types cannot be passed

type left { value }
type right { value }

func make() : right result {
    result.value = 1
}

func take(value : left) {}

func main() {
    take(make())
}
