func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}

func assert(expr : bool) if not expr exit(1)

func get_index() : i64 result {
    result = 7
}

func main() {
    var values : i8[8]
    values[get_index()] = 1
    assert(values[7] == 1)
}
