# valid index returned by a function

func assert(expr : bool) { if not expr exit(1) }

func get_index() : result {
    result = 7
}

func main() {
    var values : i8[8]
    values[get_index()] = 1
    assert(values[7] == 1)
}
