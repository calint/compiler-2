# invalid outer nested index

func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}

type group { values : i8[2] }

func main() {
    var groups : group[2]
    groups[2].values[0] = 1
}
