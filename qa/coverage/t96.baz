# escaped characters in string data

func assert(expr bool) {
    if not expr exit(1)
}

func print(len, ptr) {
    write(1, ptr, len)
}

dat hello[] i8 = "hello \"world\" \n from 'baz'\n  "

func main() {
    print(array_size_of(hello), address_of(hello))
}
