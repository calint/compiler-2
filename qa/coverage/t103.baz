# array parameter mutation is visible to the caller

func assert(expr : bool) {
    if not expr exit(1)
}

func arrarg(arg : i64[]) {
    arg[1] = 0xfe
}

func main() {
    var arr : i64[7]
    arrarg(arr)
    assert(arr[1] == 0xfe)
}
