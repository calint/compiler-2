# valid two-byte element scaling

func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}

func assert(expr : bool) if not expr exit(1)

func main() {
    var values : i16[2]
    values[1] = 1
    assert(values[1] == 1)
}
