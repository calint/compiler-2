# bounds checking must resolve an index from a field

func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}

type limits { index }

func main() {
    var values : i8[8]
    var limits : limits
    limits.index = 8
    values[limits.index] = 1
}
