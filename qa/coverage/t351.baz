# parenthesized in-range index

func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}

func assert(expr : bool) if not expr exit(1)

func main() {
    var arr : i8[8]
    var index = 3
    arr[(index + 4)] = 1
    assert(arr[7] == 1)
}
