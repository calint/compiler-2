func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}
func assert(x : bool) if not x exit(1)
func main() {
    var x : i8 = 100
    var y : i8 = 2
    var z : i8 = x / y
    assert(z == 50)
}
