# address calculation checks a computed index

func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}

func main() {
    var values : i8[8]
    var index = 8
    var address = address_of(values[index])
}
