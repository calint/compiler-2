# hexadecimal string escape decoding

func print(len : reg_rdx, ptr : reg_rsi) {
    mov(rax, 1)
    mov(rdi, 1)
    syscall()
}

dat letter : i8[] = "\x41"

func main() {
    print(array_size_of(letter), address_of(letter))
}

