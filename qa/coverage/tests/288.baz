# indexing a record element of an array parameter

type point { x, y }

func assert(expr bool) { if not expr exit(1) }

func inspect(points[] point) {
    points[1].y = 3
    assert(points[1].y == 3 and points[0].y == 0)
}

func main() {
    var points[2] point
    inspect(points)
}
