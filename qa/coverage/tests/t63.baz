
func print(str[] i8, len) {
    write(1, str, len)
}

func input(str[] i8, len) nbytes {
    nbytes = read(0, str, len)
}

dat   hello[] i8 = "hello world from baz\n"
dat   input[] i8 = "............................................................"
dat prompt1[] i8 = "enter name:\n"
dat prompt2[] i8 = "that is not a name.\n"
dat prompt3[] i8 = "hello "
dat     dot[] i8 = "."
dat      nl[] i8 = "\n"

func main(){

    print(hello, array_size_of(hello))
    loop {
        print(prompt1, array_size_of(prompt1))
        var len = input(input, array_size_of(input)) - 1    # -1 don't include the '\n'
        if len == 0 {
            break
        }
        if len <= 4 {
            print(prompt2, array_size_of(prompt2))
            continue
        }
        print(prompt3, array_size_of(prompt3))
        print(input, len)
        print(dot, array_size_of(dot))
        print(nl, array_size_of(nl))
    }
}
