# a record literal field narrows only with 'i8(...)' etc, also for a plain
# identifier that is copied directly

type pixel {
    shade i8,
    count
}

func main() {
    var x i32 = 1
    var p pixel = {x, 2}
    exit(p.shade)
}
