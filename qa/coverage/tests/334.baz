# valid upper boundary through a computed index

func assert(expr bool) { if not expr exit(1) }

func main() {
    var values[8] i8
    var index = 7
    values[index] = 5
    assert(values[7] == 5)
}
