# valid upper boundary through an array parameter

func assert(expr bool) { if not expr exit(1) }

func legacy_write(values[] i8) {
    values[7] = 1
}

func main() {
    var values[8] i8
    legacy_write(values)
    assert(values[7] == 1)
}
