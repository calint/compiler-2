# direct rbp access can save and restore the variable arena base

func main() {
    mov(rbx, rbp)
    mov(rbp, 0)
    mov(rax, rbp)
    rbp = rbx
    var observed = rax
    if observed != 0 {
        mov(rax, 60)
        mov(rdi, 1)
        syscall()
    }
}