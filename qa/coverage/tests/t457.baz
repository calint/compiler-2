# direct rbp access can save and restore the variable arena base

func main() {
    rbx = rbp
    rbp = 0
    rax = rbp
    rbp = rbx
    var observed = rax
    if observed != 0 {
        exit(1)
    }
}