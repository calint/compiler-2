# indexed values participate in a shift expression

func assert(expr bool) { if not expr exit(1) }

func fiz(x i32, shf i32) res i32 {
    res = x + x << shf
}

func main() {
    var arr[4] i32
    var ix = 1
    arr[ix] = 2
    arr[3] = 3
    var c i32 = fiz(arr[ix], arr[3])
    assert(c == 18)
}
