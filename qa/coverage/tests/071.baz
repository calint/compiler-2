# function mutation of caller data

func assert(expr bool) {
    if not expr exit(1)
}

func foozi(i) {
    i = 0xfe5e + 1
}

dat count = 1

func main() {
    assert(count == 1)
    foozi(count)
    assert(count == 0xfe5f)
}
