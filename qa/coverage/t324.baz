# address_of on an indexed array parameter

type point { x, y }

func assert(expr : bool) if not expr exit(1)

func inspect(points : point[]) {
    var addr = address_of(points[1])
    assert(addr != 0)
}

func main() {
    var points : point[2]
    inspect(points)
}
