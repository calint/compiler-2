# unary operation on array_size_of is rejected

func exit(v : reg_rdi) {
    mov(rax, 60)  # exit system call
    mov(rdi, v)   # return code
    syscall()
}

func assert(x : bool) if not x exit(1)

func main() {
    var a : i8[2]
    var n = -array_size_of(a)
    assert(n == -2)
}
