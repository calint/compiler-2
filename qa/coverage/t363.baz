# array-parameter upper-bound failure

func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}

func write(values : i8[]) {
    values[8] = 1
}

func main() {
    var values : i8[8]
    write(values)
}
