func exit(v : reg_rdi) {
    mov(rax, 60)
    mov(rdi, v)
    syscall()
}
func assert(x : bool) if not x exit(1)
func main() {
    var x : i16 = 100
    var y : i16 = 2
    var z : i16 = x / y
    assert(z == 50)
}
