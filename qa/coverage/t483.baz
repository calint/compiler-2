# copy an i32 scalar and exit with the copied value

dat prefix[3] i8 = {1, 2, 3}
dat initial i32 = 42
dat padding[5] i8

func main() {
    var source i32 = initial
    var destination i32 = source
    exit(destination)
}
