func inline exit(v:reg_rdi){
    mov(rax,60) # exit system call
    mov(rdi,v)  # return code
    syscall
}

func f(b:bool):res:bool{
    if b res=false else res=true
}

func inline main{
    var b:bool=true
    var r:bool=f(true)
    var q=1
    if r exit(1)
    r=f(b)
    if r exit(2)
    r=f(q)
    if r exit(3)
    exit(0)
}
