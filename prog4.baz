func inline exit(v:reg_rdi){
    mov(rax,60) # exit system call
    mov(rdi,v)  # return code
    syscall
}

func inline main{
    var a=-2
    var b=-a

    if not a=-1 or not b=-a exit(1)
    if not (a=-1 and b=-a) exit(1)

    exit(0)
}
