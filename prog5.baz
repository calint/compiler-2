func inline exit(v:reg_rdi){
    mov(rax,60) # exit system call
    mov(rdi,v)  # return code
    syscall
}

type point{
	x : qword,
    y : qword,
    z : qword
}

func inline main{
#    var p0 : point = 1
#    var p1 : point = 2
#    var p2 : point = 3
    var a=1
    var b=2
    exit(0)
}
