{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "baz: debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/baz",
            "args": [
                "prog.baz",
                "--stack=131072",
                "--checks=upper,lower,line",
                //"--nopt"
            ],
            "cwd": "${workspaceFolder}",
        }
    ]
}